{
  "name": "model-ia-testing",
  "version": "0.1.0",
  "private": true,
  "description": "Architecture spike for model-ia-testing",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:bdd": "cucumber-js",
    "test:browser": "playwright test",
    "evidence:viewer": "tsx src/infrastructure/evidence/serve-live-evidence.ts",
    "db:migrate": "tsx scripts/migrate.ts",
    "test:architecture": "npm run build && npm test && npm run test:bdd && npm run test:browser",
    "test:f2-ext-07:live": "tsx spike/evaluation/f2-ext-07-ollama-live-validation.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.2.0",
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.0.0",
    "@types/pg": "^8.15.5",
    "tsx": "^4.20.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
